import 'package:flutter/material.dart';

class Ragestr extends StatelessWidget {
  const Ragestr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text("Ragestr"),
    ),);
  }
}