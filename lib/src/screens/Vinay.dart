import 'package:flutter/material.dart';

class Vinay extends StatelessWidget {
  const Vinay({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Vinay"),),);
  }
}